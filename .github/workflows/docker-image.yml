name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  deploy:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: Deploy using Docker Compose
      run: |
        cd /home/elder/Code/elder/test-repository
        docker-compose up -d
